description "Nomad server process"

start on (local-filesystems and net-device-up IFACE="{{ nomad_iface }}")
stop on runlevel [!12345]

respawn

setuid root
setgid root

exec nomad agent -server -config /etc/nomad/server.hcl

